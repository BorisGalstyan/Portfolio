# Мультинейросеть — веб‑платформа с несколькими AI‑ассистентами (MVP)

Демо‑проект веб‑приложения, которое даёт пользователю единый интерфейс для нескольких тематических AI‑ассистентов: юридический навигатор, автопомощник, ПК‑мастер, дом‑мастер и медицинский переводчик симптомов.  
Архитектура и интерфейс спроектированы под реальные ТЗ на создание «мультинейросети» с интеграцией российских LLM (YaGPT/GigaChat) через API. Сейчас используется демонстрационный ответ, но места подключения реальной модели уже заложены. [memory:48]

---

## Основные возможности

- 📥 **Вход через email и промокод**  
  Простая форма авторизации, после которой пользователь попадает в личный кабинет с доступными модулями.

- 🧩 **Модульные AI‑ассистенты**  
  5 тематических помощников: юрист, авто, ПК, дом и медицина. Каждый модуль имеет своё название и набор готовых промптов (шаблонных запросов).

- 💬 **Современный чат‑интерфейс**  
  Bubble‑чат в тёмной теме с прокруткой истории, быстрый выбор шаблонов, отправка произвольных вопросов.

- 🎨 **Адаптивный UI на Tailwind CSS**  
  Карточки модулей, тёмная тема, аккуратное оформление для демонстрации заказчикам и включения в портфолио.

- 🔌 **Готовность к интеграции с LLM**  
  Backend API спроектирован так, что mock‑ответ легко заменить на реальный вызов YaGPT/GigaChat (через `requests`/`httpx` и промптинг по модулю).

---

## Технологии

**Backend**

- Python 3.x  
- FastAPI — HTTP API и раздача фронтенда  
- Pydantic — валидация входных данных

**Frontend**

- Чистый JavaScript (без фреймворков)  
- Tailwind CSS (подключён через CDN)  
- Адаптивная вёрстка, dark‑theme, карточки модулей и bubble‑чат

Проект специально сделан максимально лёгким для запуска и чтения кода (подходит как учебный пример и как демо для клиентов). [memory:31]

---


Папку `media/` можно не клонировать на прод, она нужна для портфолио и превью.  

---

## Установка и запуск (локально)

1. Клонировать репозиторий:

git clone https://github.com/<YOUR_USERNAME>/Portfolio.git
cd Portfolio/multineiroset_ai_platform

text

2. Установить зависимости (желательно в виртуальное окружение):

pip install -r requirements.txt

text

3. Запустить сервер разработки:

uvicorn main:app --reload

text

4. Открыть в браузере:

http://127.0.0.1:8000

text

5. Ввести любой email и любой промокод (например, `test@example.com` и `DEMOJURIST`) — после этого откроется личный кабинет с модулями и чат‑интерфейсом.

---

## Где подключать реальный AI (YaGPT/GigaChat)

Сейчас проект использует простую заглушку‑ответ. Точка расширения лежит в обработчике чата:

@app.post("/chat/{module_id}")
async def chat(module_id: str, req: ChatRequest):
if module_id not in MODULES:
raise HTTPException(status_code=404, detail="Модуль не найден")

text
base = MODULES[module_id]["name"]
text = (
    f"Это ответ от ассистента «{base}» на ваш запрос. "
    f"В реальном сервисе здесь будет ответ от YaGPT/GigaChat. "
    f"Ваш вопрос: «{req.message}»."
)
return {"response": text}
text

Чтобы подключить реальный LLM‑провайдер, достаточно:

1. Добавить клиент (например, через `httpx` или официальный SDK).  
2. Для каждого `module_id` сформировать `system_prompt` (роль ассистента: юрист, автоэксперт и т.д.).  
3. Заменить `text = ...` на вызов LLM и вернуть его ответ.

Таким образом, весь UI и маршрутизация уже готовы, остаётся только интегрировать выбранный API. [memory:31]

---

## Идеи для развития

- Авторизация с реальными промокодами и ролями пользователей  
- Ограничения по количеству диалогов и токенов на пользователя  
- Логирование запросов и ответов для аналитики  
- Поддержка нескольких языков интерфейса  
- Отдельная админ‑панель для управления модулями и шаблонами промптов

---

## Demo

- **Скриншоты**: см. `media/screenshots`.  
- **Видео‑демо**: `media/demo/multineiroset_demo.mp4` (короткий ролик, показывающий сценарий входа, выбора модуля и диалога).

---

## English Version

### Multineiroset — Multi‑Assistant AI Web Platform (MVP)

This is a demo web application that provides a single interface for several domain‑specific AI assistants: legal, car maintenance, PC support, home handyman and medical symptom translator.  
The architecture and UI are designed according to real‑world requirements for a “multi‑assistant” platform that integrates Russian LLMs (YaGPT/GigaChat) via API. Currently a mock response is used, but the backend is ready for plugging in a real model.

#### Features

- Simple login via email + promo code (demo mode)  
- Multiple AI modules (cards in a dashboard)  
- Modern chat UI with dark theme and prompt shortcuts  
- Lightweight FastAPI backend + plain JS + Tailwind CSS  
- Clear extension point for integrating any LLM provider

#### Quick start

git clone https://github.com/<YOUR_USERNAME>/Portfolio.git
cd Portfolio/multineiroset_ai_platform
pip install -r requirements.txt
uvicorn main:app --reload

open http://127.0.0.1:8000
text

Enter any email and promo code to access the dashboard and start chatting with the assistants.

---